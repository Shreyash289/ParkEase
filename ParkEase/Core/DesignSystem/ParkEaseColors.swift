import SwiftUI

enum ParkEaseColors {
    static let canvas = Color("Canvas")
    static let card = Color("Card")
    static let graphite = Color("Graphite")
    static let secondaryText = Color("SecondaryText")
    static let line = Color("Line")
    static let available = Color("AvailableGreen")
    static let warning = Color("WarmOrange")
    static let occupied = Color("OccupiedRed")
    static let route = Color("RouteTeal")

    static let slotAvailable = Color(red: 0.22, green: 0.55, blue: 0.34)
    static let slotLimited = Color(red: 0.89, green: 0.50, blue: 0.18)
    static let slotFull = Color(red: 0.72, green: 0.22, blue: 0.18)
}
